# 需求说明文档模板

## 1. 文档背景

### 1.1 项目背景
<!-- 从原型工程目录名、README、package.json 等推断项目定位 -->

### 1.2 文档目的
本文档基于原型工程代码逆向生成，旨在将代码中隐含的产品逻辑、交互规则和数据需求显性化为结构化需求说明，为后续开发、测试和产品迭代提供可追溯的需求基线。

### 1.3 适用范围
<!-- 列出本次解析覆盖的页面/模块范围 -->

## 2. 目标用户

### 2.1 用户角色定义

| 角色ID | 角色名称 | 描述 | 权限特征 |
|--------|---------|------|---------|
| R-001 | <!-- 角色名 --> | <!-- 从页面权限逻辑推断 --> | <!-- 可见/可操作的功能范围 --> |

### 2.2 使用场景

| 场景ID | 场景名称 | 角色ID | 触发条件 | 目标 |
|--------|---------|--------|---------|------|

## 3. 核心业务流程

### 3.1 流程概述
<!-- 从路由跳转和页面间数据传递推断主业务流程 -->

### 3.2 主流程

```
步骤1: [用户操作] → [系统响应] → [页面状态变化]
步骤2: ...
步骤N: [结束状态]
```

### 3.3 分支流程
<!-- 从条件渲染和备选操作推断 -->

## 4. 功能清单

### 4.1 功能模块树

```
项目名称
├── 模块A
│   ├── 功能A-1 [F-001] [优先级] [🟢/🟡/🔴]
│   └── 功能A-2 [F-002] [优先级] [🟢/🟡/🔴]
├── 模块B
│   ├── 功能B-1 [F-003] [优先级] [🟢/🟡/🔴]
│   └── ...
```

### 4.2 功能-页面映射

| 功能ID | 功能名称 | 所属页面 | 页面路由 | 关联代码文件 |
|--------|---------|---------|---------|------------|

## 5. 功能说明

### 5.x [功能名称] [F-xxx]

**所属页面**：[页面名称]（[路由路径]）
**功能概述**：[一句话描述该功能做什么]

#### 基本事件流

| 步骤 | 操作者 | 操作 | 系统响应 |
|------|--------|------|---------|
| 1 | 用户 | [触发动作] | [系统行为] |
| 2 | 系统 | — | [处理逻辑] |
| 3 | ... | ... | ... |

#### 备选事件流

| 分支ID | 触发条件 | 流程 |
|--------|---------|------|
| ALT-1 | [条件] | [分支路径步骤] |

#### 异常事件流

| 异常ID | 触发条件 | 系统处理 | 用户恢复方式 |
|--------|---------|---------|------------|
| ERR-1 | [错误场景] | [错误处理] | [如何恢复] |

#### 需求规则

| 规则ID | 规则类型 | 规则描述 | 代码依据 |
|--------|---------|---------|---------|
| RL-1 | 业务规则 | [具体规则] | [代码位置] |
| RL-2 | 校验规则 | [校验逻辑] | [校验代码位置] |
| RL-3 | 权限规则 | [权限要求] | [权限代码位置] |

#### 关联功能点

| 关联类型 | 功能点ID | 功能点名称 | 关联说明 |
|---------|---------|---------|---------|
| ⬅ 前置依赖 | <!-- F-xxx --> | <!-- 功能名 --> | <!-- 依赖说明 --> |
| ➡ 后续影响 | <!-- F-xxx --> | <!-- 功能名 --> | <!-- 影响说明 --> |

#### 字段说明

> 按字段类型覆盖完整规则维度。参照《需求字段规则标配识别点》定义。

**通用维度（所有字段）**：

| 字段名 | 中文名 | 数据类型 | 控件类型 | 必填 | 可编辑 | 数据唯一性 | 默认值 | 占位提示 | 校验规则 | 校验触点 | 验证提示语 | 自动取值逻辑 | 隐藏字段 | 关联字段 |
|--------|--------|---------|---------|------|--------|-----------|--------|---------|---------|---------|---------|------------|---------|---------|
| <!-- fieldName --> | <!-- 中文名 --> | <!-- String/Number/Date/Boolean --> | <!-- 输入框/下拉选择框/文本域/日期选择器/数字输入框/开关/复选框 --> | 是/否 | 是/否（条件） | 是/否（范围） | <!-- 默认值 --> | <!-- 占位文本 --> | <!-- 校验规则 --> | <!-- blur/submit --> | <!-- 错误提示 --> | <!-- 取值逻辑 --> | 是/否（逻辑） | <!-- F-xxx.field --> |

**按控件类型补充维度**：

| 控件类型 | 额外维度 |
|---------|---------|
| 输入框 | 长度限制（最大/最小/超出处理）、输入格式（如只允许数字和小数点、不允许特殊字符） |
| 下拉选择框 | 选项列表（每个选项的具体内容）、单选/多选 |
| 文本域 | 长度限制（字数）、是否支持拉宽（resizable） |
| 日期/时间选择器 | 格式（如 yyyy-MM-dd HH:mm:ss）、可选范围限制 |
| 数字输入框 | 最小值/最大值、小数位数、步长 |
| 开关/复选框 | 触发后的关联影响（如启用后自动关联哪些字段） |

#### 数据需求说明

| 数据实体 | 字段列表 | 状态流转 | 存储要求 |
|---------|---------|---------|---------|
| <!-- 实体名 --> | <!-- 关键字段 --> | <!-- 状态机 --> | <!-- 前端缓存/持久化 --> |

#### 代码溯源

| 内容 | 代码文件 | 行号范围 |
|------|---------|---------|
| 模板结构 | [文件路径] | [行号] |
| 交互逻辑 | [文件路径] | [行号] |
| 数据定义 | [文件路径] | [行号] |
| API调用 | [文件路径] | [行号] |

## 6. 接口需求

### 6.1 API清单

| API编号 | 接口名称 | 请求方法 | URL路径 | 调用位置 | 说明 |
|---------|---------|---------|---------|---------|------|

### 6.2 接口详情

#### API-xxx: [接口名称]

**请求**：
- Method: [GET/POST/PUT/DELETE]
- URL: [路径]
- Headers: [自定义头]
- Body: [请求参数结构]

**响应**：
- 成功状态码: 200
- 响应结构: [JSON结构]

**依赖关系**：[该接口依赖的其他接口或前置条件]

## 7. 安全需求

### 7.1 权限控制

| 资源 | 角色 | 操作权限 | 代码依据 |
|------|------|---------|---------|

### 7.2 数据脱敏

| 数据项 | 脱敏规则 | 应用场景 | 代码依据 |
|--------|---------|---------|---------|

### 7.3 输入防护

| 输入点 | 防护类型 | 规则 | 代码依据 |
|--------|---------|------|---------|
